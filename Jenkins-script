pipeline {
    agent any
    
    environment {
        PATH = "/opt/apache-maven-3.6.3/bin:$PATH"
    }
    
    stages {
        stage('Git Checkout') {
            steps {
                git 'https://github.com/hulakv/hello_world.git'
            }
        }
        stage ("Maven Buid") {
            steps {
                sh "mvn clean package"
            }
        }
   }
 
 }
 
